# HPP-SAM 公式、训练、显存与工程修复检查

> 文档状态：代码审计与修复设计稿  
> 适用仓库：HPP-SAM  
> 基线提交：`505ace7 Initial HPP-SAM research codebase`  
> 审计范围：双曲几何公式、HRA、双向交叉注意力、交互训练、DeepSpeed/ZeRO-3、PartNet/PartNeXt 数据管线、显存、数值稳定性、测试与论文实验设计。

---

## 1. 执行摘要

HPP-SAM 的研究方向具有较高的工程可实现性：在 Point-SAM 的可提示 3D 分割框架中，将提示表示映射到 Poincaré 球，并使用提示的双曲半径调节 prompt-to-point 注意力的集中程度，能够形成一个合理的“交互提示—层级粒度—部件分割”假设。

当前阶段的综合判断如下：

| 维度 | 判断 |
|---|---|
| 想法能否工程实现 | 高，约 8/10 |
| 当前代码能否稳定训练 | 中低，存在若干阻断性错误 |
| 当前结果能否直接证明论文公式 | 低，约 4/10 |
| 完成一致性修复后的研究潜力 | 中高，约 6.5～7/10 |

当前最优先的问题不是继续扩大模型或增加训练资源，而是先修复以下四类基础问题：

1. **DeepSpeed 梯度累积和学习率调度器语义错误**，可能导致梯度被重复缩小、实际更新周期异常、学习率恒定。
2. **双曲空间不一致**：提示分支与注意力层使用独立可学习曲率，并且球内点经过普通欧氏线性层后未重新映射到球内。
3. **PartNeXt mask 数无上限和降采样后空 mask**，会导致随机 OOM、默认 collate 失败和 IoU `0/0`。
4. **十轮交互同时保留十个计算图**，是激活显存近似线性增长的主要结构性原因。

在上述问题修复前，训练曲线即使看起来正常，也不能确认优化的是预期目标；实验结果也不能可靠地归因于双曲几何。

---

## 2. 当前方法的目标与合理定位

### 2.1 当前代码试图实现的流程

当前 HPP-SAM 的核心流程可以概括为：

1. 输入点云坐标与特征：
   \[
   X \in \mathbb{R}^{B\times N\times 3},\qquad F\in\mathbb{R}^{B\times N\times C}.
   \]
2. Point Cloud Encoder 将点云分组为 patch token，并通过 EVA Transformer 编码：
   \[
   Z_{pc}\in\mathbb{R}^{B\times L\times D}.
   \]
3. 交互点击经过 Point Encoder 得到欧氏提示：
   \[
   p_i\in\mathbb{R}^{D}.
   \]
4. HyperPromptBranch 使用指数映射与 HRA 将提示变换到 Poincaré 球：
   \[
   q_i=\exp_0^c(\alpha p_i),
   \qquad
   \widetilde q_i=W\otimes_c q_i.
   \]
5. 使用双曲距离定义提示半径：
   \[
   r_i=d_c(\widetilde q_i,0).
   \]
6. 将半径映射为注意力集中参数：
   \[
   \tau_i=\tau_{min}+(\tau_{max}-\tau_{min})
   \sigma(a r_i+b).
   \]
7. 使用距离注意力：
   \[
   A_{ij}=\operatorname{softmax}_j(-\tau_i d_c(q_i,k_j)).
   \]
8. 预测 mask，根据预测错误继续采样点击，重复最多十轮。

### 2.2 更准确的当前方法名称

按照现有 value 聚合实现，当前方法更准确的描述应是：

> **Hybrid radius-conditioned geodesic attention with Euclidean/tangent-space value aggregation**

即“使用双曲距离产生注意力权重，但 value 仍在欧氏/原点切空间聚合”。在 value 聚合真正保持流形结构以前，不建议声称整个 cross-attention 都是严格双曲的。

### 2.3 新颖性判断

已有相关方向包括：

- HyperET 已使用 HRA、对角/块对角 Möbius scaling；
- 双曲图像分割已用于层级类别；
- 双曲 3D 点云分割已用于 part-whole 层级；
- Point-SAM 已实现可交互 3D 点云分割。

目前仍有潜在组合创新的部分是：

> 将交互提示的双曲半径显式作为粒度控制信号，并注入可提示 3D 分割的 geodesic cross-attention。

但论文必须排除以下替代解释：

- 提升仅来自额外参数；
- 提升仅来自动态 temperature；
- 提升仅来自欧氏距离注意力；
- 提升仅来自局部 top-k 邻域；
- 提升仅来自正则化；
- 提升仅来自更换 tokenizer 或 backbone。

---

## 3. 双曲基础公式审计

### 3.1 基本正确的部分

`hpp_sam/model/hyper_ops.py` 中以下主公式总体符合 Ganea 等人的 Poincaré ball 神经网络定义：

- 原点指数映射 `exp0`；
- 原点对数映射 `log0`；
- Möbius addition；
- Poincaré distance；
- Möbius matrix-vector multiplication；
- `radius = d_c(q, 0)`。

标准定义中，曲率参数取 \(c>0\)，Poincaré 球为：

\[
\mathbb{D}_c^n=\{x\in\mathbb{R}^n:c\|x\|^2<1\}.
\]

欧氏坐标半径边界为：

\[
\|x\|<1/\sqrt c.
\]

当 `c=0.01` 时，欧氏球边界半径为 10。因此配置注释中“`radius≈1` 接近边界”的说法错误；它仍处于相对靠近原点的区域。还需要区分代码中的 `radius` 究竟表示欧氏范数还是测地距离；当前 HRA 输出使用的是测地距离。

### 3.2 Möbius matrix-vector multiplication 的文档理解错误

代码注释中曾将以下两者描述为“一般情况下不等价”：

\[
M\otimes_c x
\quad\text{与}\quad
\exp_0^c(M\log_0^c(x)).
\]

该说法不正确。Ganea 2018 中 Möbius matrix-vector multiplication 本来就定义为：

\[
M\otimes_c x:=\exp_0^c(M\log_0^c(x)).
\]

代码中的闭式实现总体正确，错误主要在注释和设计理解。论文与代码文档应统一修正，避免把同一运算错误地描述成两个不同方案。

---

## 4. 公式与实现不一致的问题

### 4.1 CRITICAL：多个独立可学习曲率导致跨流形计算

#### 当前行为

当前至少有以下位置分别创建 `HyperOps`：

- `HyperPromptBranch`；
- 每个 `HyperCrossAttention`；
- `TwoWayAttentionBlock` 内部辅助双曲操作。

如果这些曲率都可学习，训练后通常有：

\[
c_{prompt}\ne c_{attn,1}\ne c_{attn,2}\ne\cdots
\]

提示坐标是在 \(\mathbb D_{c_{prompt}}\) 上产生的，却被直接放入 \(d_{c_{attn}}\) 中计算。相同的欧氏坐标在不同曲率的球上并不表示同一个几何点。

#### 风险

- 距离与半径不再属于同一几何；
- temperature 对应的 radius 与 attention 使用的 distance 不匹配；
- 曲率独立漂移后，论文公式无法成立；
- 每层曲率可能通过数值缩放“作弊”，而不是学习层级结构。

#### 推荐修复

优先方案：

1. 第一版使用全模型固定 `c=0.01`，建立稳定基线；
2. 第二版使用全模型共享的单个可学习曲率；
3. 如果必须每层不同曲率，显式执行：
   \[
   x_{c_2}=\exp_0^{c_2}(\log_0^{c_1}(x_{c_1})).
   \]

#### 验证测试

- 所有 attention 与 prompt branch 读取同一曲率对象；
- 更新一次 optimizer 后检查曲率对象 identity；
- 对固定坐标比较不同曲率下的距离，确保没有隐式混用；
- 检查共享曲率梯度非零且有限。

### 4.2 CRITICAL：球内 query/key 经过普通欧氏投影后直接计算双曲距离

#### 当前行为

`HyperCrossAttention.forward` 对输入执行普通线性层：

```python
q = self.q_proj(query)
k = self.k_proj(key)
```

随后把 `q` 或 `k` 直接视为球内点。普通 affine transformation，特别是带 bias 的线性层，不保持：

\[
c\|x\|^2<1.
\]

而且将 embedding 按 head 切分后，每个 head 的坐标也没有重新映射到相应 Poincaré 球。

#### 推荐修复 A：切空间投影

```text
q_ball
→ log0_c(q_ball)
→ q_proj_euclidean
→ split heads
→ exp0_c / project_c
→ q_ball_projected
```

对已经是双曲点的 key 使用同一流程。对欧氏 point-cloud key，可先线性变换，再 `exp0/project`。

#### 推荐修复 B：Möbius linear

定义：

\[
W\otimes_c x=\exp_0^c(W\log_0^c(x)).
\]

若使用 bias，需要使用 Möbius addition，而不是普通欧氏加法。

#### 必须检查

```python
assert (c * q.pow(2).sum(-1) < 1).all()
assert (c * k.pow(2).sum(-1) < 1).all()
```

训练时不建议每步同步执行 assert，可在 debug/单元测试中执行，并在正式训练中使用低频统计。

### 4.3 CRITICAL：所谓 tangent mean 存在 exp/log 抵消

#### 当前行为

value 路径近似执行：

\[
v\rightarrow \exp_0(v)\rightarrow\log_0(\exp_0(v))
\rightarrow\sum_j a_jv_j
\rightarrow\exp_0\rightarrow\log_0.
\]

在没有数值截断的理想情况下：

\[
\log_0(\exp_0(v))=v,
\]

所以该路径退化为普通欧氏加权平均。当前双曲几何主要作用在 logits，而不是 value 聚合。

#### 两种合理处理

1. **接受混合设计**：删除无意义的 exp/log 往返，并在论文中明确 value 是 tangent/Euclidean aggregation。
2. **实现真正流形聚合**：在 query 的局部切空间做 log map、加权平均和 exp map，或使用 Einstein midpoint/Fréchet mean，并报告计算开销与稳定性。

第一阶段建议选择方案 1，因为它更稳定，也更容易证明 geodesic logits 的独立贡献。

### 4.4 HIGH：反向 point-to-prompt attention 丢失逐提示 temperature

prompt-to-point 方向中，\(\tau_i\) 与第 \(i\) 个 prompt query 对齐，逻辑成立。

point-to-prompt 方向中，query 是大量 point token，prompt 变成 key。当前代码在长度不匹配时对所有 prompt 的 temperature 求均值并广播：

\[
\bar\tau=\frac1P\sum_i\tau_i.
\]

这样反向分支不再具有 per-prompt 粒度控制。

候选方案：

- 只在 prompt-to-point 分支使用 radius conditioning；
- 在反向分支定义 key-wise logits：
  \[
  \ell_{ji}=-\tau_i d(q_j,k_i);
  \]
- 将反向分支恢复为普通欧氏 attention，并作为稳定基线。

三者需要消融，不能默认双向注入一定更好。

### 4.5 HIGH：可学习曲率梯度被部分切断

`mobius_add` 中将曲率执行 `.detach().item()`：

- 切断该部分对 curvature 的梯度；
- 强制 GPU→CPU 同步；
- 使 distance 外层可能仍有曲率梯度，但梯度已不是完整公式的梯度。

应全程保留 tensor curvature，并避免 Python 标量分支。

### 4.6 HIGH：parallel transport 公式不标准

当前 `parallel_transport` 实现并非标准 Poincaré transport。标准形式通常包含 conformal factor 比值和 gyration：

\[
P_{x\to y}^c(v)=\frac{\lambda_x^c}{\lambda_y^c}
\operatorname{gyr}[y,-x]v.
\]

当前实现通过 log/difference 重新定向，一般不保持线性和方向。如果函数当前未使用，建议先删除或标记为未实现；若未来使用，应直接对照 Geoopt 官方公式重写并增加数值测试。

### 4.7 HIGH：diagonal HRA 路径会发生参数签名错误

`HyperPromptBranch.forward` 总是调用：

```python
self.hra_ws(q, hang_debug=hang_debug)
```

但 `HyperScaleWS_Diagonal.forward` 只接收 `x_ball`。默认 block-diagonal 不触发，一旦配置切换为 diagonal 就会 `TypeError`。

修复方式：统一两种实现的 forward 签名，并增加 diagonal/block-diagonal 参数化测试。

### 4.8 MEDIUM：temperature 的单调解释不受约束

当前 \(a\) 可训练为负数，使：

\[
\frac{d\tau}{dr}<0.
\]

这会反转“半径大代表细粒度、注意力更尖锐”的核心解释。推荐：

```python
a = F.softplus(raw_a) + eps
```

如果 `alpha` 也承担正向尺度解释，也应使用 `softplus` 或正值参数化，避免变负后翻转方向。

此外，初始 `a=0.5, b=0` 且 `r≥0` 时，sigmoid 输入不小于 0，导致初始 \(\tau\) 不会进入配置所声称的低温平滑区域。需要重新设计初始化，让整个 `[tau_min, tau_max]` 在实际 radius 范围内可达。

### 4.9 MEDIUM：`logit_scale` 是数学上的死参数

若对同一 softmax 行的每个 key 都加相同标量 \(s\)：

\[
\operatorname{softmax}(z_j+s)=\operatorname{softmax}(z_j).
\]

因此当前标量 `logit_scale` 完全抵消，理论梯度为零。`self.scale` 也没有用于距离 logits。

修复选择：

- 删除这两个无效参数；
- 改为 per-head scale；
- 改为依赖 query/key 的 bias；
- 若需要距离缩放，使用受约束的正值 `distance_scale`。

### 4.10 MEDIUM：learnable curvature 初始化参数化不准确

如果内部使用：

```python
c = softplus(raw_c)
```

那么从目标曲率 \(c_0\) 初始化 `raw_c` 时应使用 softplus inverse，而不是简单 `log(c_0)`。简单 `log` 在 `c=0.01` 附近误差较小，但在 `c≈1` 时偏差明显。setter 同样应使用 inverse-softplus。

### 4.11 MEDIUM：radius 正则没有直接监督粒度

当前正则大致为：

\[
L_{mean}=\mathbb E[(r-r_0)^2],
\qquad
L_{div}=-\operatorname{std}(r).
\]

它只要求 radius 不坍缩且彼此不同，不能保证 radius 与 part 层级或点击粒度有关。`-std` 还可能鼓励 radius 走向极端，必须由更强边界约束抵消。

建议先做相关性分析，再决定是否加入弱监督：

- GT hierarchy depth；
- mask area ratio；
- 当前点击轮数；
- 点击前后的 IoU gain；
- parent/child part 关系。

---

## 5. 数值稳定性与可审计性

### 5.1 静默 Euclidean fallback 会污染训练目标

当前多个双曲算子捕获 `RuntimeError` 后：

- 回退到 Euclidean distance；
- 返回输入；
- 对 non-finite 值执行 `nan_to_num`；
- 继续完成 batch。

这会使同一次训练在不同 batch、不同层甚至不同样本之间混用不同几何，而日志中没有明确统计。最终结果不可审计，也不能确认模型是否真的使用双曲路径。

推荐：

- 训练阶段默认 fail-fast；
- 或至少记录每种 fallback 的计数、层名、rank、step；
- fallback 比率非零时将该实验标记为无效；
- 推理阶段可以保留受控 fallback。

### 5.2 双曲核心建议使用 float32

即使主网络使用 bf16，以下运算建议在局部 autocast-disabled 区域使用 float32：

- norm 与球边界检查；
- `artanh`；
- Möbius denominator；
- Poincaré distance；
- exp/log map；
- curvature 与 radius 计算。

输出可以再转换为 bf16。这样通常比全链路 bf16 稳定，额外显存远小于十轮激活图带来的开销。

### 5.3 边界 clamp 可能导致梯度归零

频繁 `clamp` 或 `project` 虽能防止 NaN，但贴近边界的点会出现梯度截断。需要监控：

- `max(sqrt(c) * ||x||)`；
- 被 project 的 token 比例；
- curvature、radius、temperature 分布；
- non-finite 发生位置；
- HRA 输出与输入范数。

### 5.4 频繁同步严重拖慢训练

`HyperPromptBranch.forward` 中存在无条件 print、`.item()`、`isfinite().all()` 和 `torch.cuda.synchronize()`。训练主循环也在每个 batch 的 metric 前同步。

这些操作不会直接制造大量长期显存，但会：

- 破坏 kernel overlap；
- 让每轮 prompt 强制等待 GPU；
- 放大 ZeRO/NCCL rank skew；
- 使训练看起来像 hang；
- 严重降低吞吐。

应全部受 debug flag 控制，正式训练默认关闭。

---

## 6. 训练算法问题

### 6.1 CRITICAL：DeepSpeed 梯度累积实现错误

#### 当前行为

DeepSpeed config 已设置：

```json
"gradient_accumulation_steps": GAS
```

训练循环又执行：

```python
model.backward(loss / GAS)
if external_flag:
    model.step()
```

#### 为什么错误

DeepSpeed Engine 正常语义是每个 microbatch 都调用：

```python
engine.backward(loss)
engine.step()
```

Engine 内部判断当前是否为 gradient accumulation boundary，并在真正边界才更新参数。

当前实现可能造成：

- loss 被手动和 DeepSpeed 双重缩放；
- Engine micro-step 只在外层 flag 时推进；
- 有效更新周期从 GAS 放大为版本相关的异常周期，最坏可能接近 GAS²；
- 梯度过小，表现为 loss 长期不动；
- 日志中的 global step 与真实 optimizer step 不一致。

#### 正确修复

DeepSpeed 路径每个成功 microbatch：

```python
model.backward(loss)
model.step()
```

不要手动除以 GAS，不要用外层 flag 阻止 `model.step()`。如需保存/日志，只使用：

- `model.is_gradient_accumulation_boundary()`；
- `model.global_steps`；
- optimizer step 前后显式记录。

DDP/FSDP 路径可以保留独立的手动 accumulation 逻辑，但必须与 DeepSpeed 分开实现。

### 6.2 CRITICAL：DeepSpeed 学习率调度器没有真正运行

当前 `deepspeed.initialize()` 没有接收正确的 scheduler，之后创建的 placeholder `step()` 是 no-op。配置里的 warmup、milestone、decay 因此不生效，学习率可能始终为 `3e-4`。

修复选择：

1. 在 initialize 前实例化 scheduler，并通过 `lr_scheduler=` 传入；
2. 完全使用 DeepSpeed config 的 scheduler；
3. 确保 scheduler 仅在真实 optimizer boundary 更新一次。

需要日志同时记录：

- micro step；
- optimizer/global step；
- 当前 LR；
- 是否 accumulation boundary。

### 6.3 HIGH：radius diversity 单元素标准差产生 NaN

`radius.std()` 默认 `unbiased=True`。当某一轮只有单个 radius 时，无偏样本标准差分母为 0，结果是 NaN。

修复：

```python
if radius.shape[-1] < 2:
    return radius.new_zeros(())
std = radius.std(dim=-1, unbiased=False)
loss = -std.mean()
```

还应明确 diversity 是在：

- 同一样本的不同 prompts 内计算；
- 还是不同 masks 间计算；
- 还是不同层级间计算。

不应简单把 batch、mask、prompt 全部混为一个总体标准差。

### 6.4 HIGH：十轮交互同时保留十个独立计算图

当前每轮都执行：

- Point Prompt Encoder；
- Mask Encoder；
- HRA；
- 四层 TwoWayTransformer；
- mask upscaling 与 loss。

虽然点击采样和 mask 输入被 detach/no-grad，轮与轮之间没有完整 BPTT，但每轮 loss 都参与最终总 loss，所以十轮的独立计算图必须保留到 backward。

激活显存近似：

\[
M_{act}\approx
M_{encoder}+T\cdot M_{mask+HRA+decoder},
\qquad T=10.
\]

#### 推荐方案：随机目标深度 rollout

每个 batch 随机采样 \(t\in[1,10]\)：

1. 前 \(t-1\) 轮使用 `torch.no_grad()` 模拟点击；
2. 第 \(t\) 轮开启梯度并计算监督；
3. 根据目标采样分布对 loss 做重要性加权；
4. 固定一小部分 batch 使用完整 10 轮，作为后段交互校准。

建议采样分布提高后段轮次概率，例如均匀采样或偏向 5～10 轮，而不是长期只训练前三轮。

该方法是 truncated rollout，不与完整十轮监督严格等价，必须做：

- full-10 vs random-depth；
- IoU@1/3/5/10；
- NoC85/90；
- 峰值显存与吞吐对比。

### 6.5 MEDIUM：refinement iteration 每次实际固定

当前 forward 内部每次重新创建 Generator，并以固定 seed 初始化，因此所谓随机 `sampled_iter` 在每次 forward 中都一样。分布式 broadcast 只是把同一个常数广播给各 rank。

后果：

- 训练长期只覆盖某个固定 refinement 模式；
- 注释所声称的随机 mask refinement 不成立；
- 最后轮的点击/不点击分布可能与评估不一致。

修复：

- 使用 rank 0 的持久 RNG；
- seed 组合 `base_seed + global_step`；
- 将采样结果 broadcast 给所有 rank；
- 日志记录每轮采样频率，确认覆盖分布。

### 6.6 MEDIUM：第一轮与后续轮 loss 设计需要消融

当前第一轮使用 best-of-multimask，后续轮使用单 mask，符合 SAM 类交互分割的常见做法。后续轮权重更高也有直觉依据。

但需要确认：

- 第一轮 multimask 是否压制 HRA 学习；
- 后续轮权重是否导致早期点击性能下降；
- radius regularization 是否应该对各轮等权；
- temperature/radius 是否应在首轮没有历史 mask 时单独处理。

建议报告每轮独立 loss 和梯度范数，而不是只有总 loss。

### 6.7 MEDIUM：DeepSpeed optimizer offload fallback 可能不兼容

当 `DeepSpeedCPUAdam` 构建失败时，代码回退到普通 `torch.optim.AdamW`，但 ZeRO optimizer offload 配置可能仍然开启。部分 DeepSpeed 版本会拒绝这种组合或表现异常。

建议：

- offload 开启时，将 `DeepSpeedCPUAdam` 可用性作为启动前硬检查；
- 不可用则明确关闭 optimizer offload，而不是静默换 optimizer；
- smoke test 必须覆盖真实训练 optimizer 与 config 组合。

### 6.8 LOW：训练输出对象生命周期过长

`last_train_outputs = outputs` 会在 batch 后继续持有输出对象。正常 backward 后 saved tensors 多数已释放，因此它不是首要 OOM 根因，但会保留 graph 节点与引用，增加调试复杂度。

建议只保存诊断所需的 detach 后摘要，epoch 末诊断不要持有完整训练输出图。

---

## 7. 数据管线问题

### 7.1 HIGH：PartNeXt mask 数无上限

PartNet 配置使用 `RandomSampleMask(2)`，PartNeXt 只执行 `FilterInvalidMasks`，没有限制 \(M\)。模型中大量张量按 \(B\times M\) 展开。

后果：

- 复杂样本随机 OOM；
- 不同 rank 的计算时间差异极大；
- ZeRO-3 collective 出现 rank skew；
- val batch_size > 1 时，不同样本的 M 不同，默认 collate 无法 stack；
- 训练吞吐由最复杂样本决定。

第一阶段建议训练和验证都固定：

```yaml
- _target_: hpp_sam.datasets.transforms.RandomSampleMask
  num_samples: 2
```

后续可试 4，但必须做显存和效果消融。

### 7.2 HIGH：`RandomSampleMask` 在 mask 太少时可能报错

当 `num_masks < num_samples` 时，当前实现用 `replace=False` 从已有 masks 中补足。如果缺口大于现有 mask 数，会触发 `np.random.choice` 错误。

修复：

- 缺口大于可选数时使用 `replace=True`；
- 或循环重复已有 masks；
- 更好地返回 valid-mask 标志，避免重复 mask 被当成独立监督样本统计。

### 7.3 HIGH：先过滤 mask、后降采样会产生空 GT

当前 PartNeXt 顺序：

```text
FilterInvalidMasks(min_points=16)
→ RandomSamplePoints(3072)
```

一个原始仅有 16 个前景点的 part，在从 10000 点随机降采样后可能保留 0 个前景点。

后果：

- 无法采有效正点击；
- focal/dice 目标退化；
- IoU union 可能为 0；
- IoU head loss 变 NaN；
- 多卡时某 rank 提前报错，诱发 collective hang。

推荐方案：

1. 先选择要监督的 masks；
2. 使用 mask-aware/stratified 点采样，保证每个选中 mask 至少保留 `min_fg_after_sample` 个前景点；
3. 降采样后再次过滤；
4. 若全部 mask 无效，重采该 example，而不是强行保留 mask 0；
5. 训练前统计空 mask 率。

### 7.4 HIGH：`compute_iou` 对 empty-empty 没有定义

当前：

\[
IoU=\frac{|P\cap G|}{|P\cup G|}.
\]

当预测和 GT 都为空时，分母为 0。必须明确任务语义：

- empty-empty 记为 1；
- 或该 mask 记为 invalid 并排除；
- 不能无声产生 NaN。

推荐：训练阶段不允许空 GT；metric 层面使用 valid mask，并对 union 做 `clamp_min(1)` 或显式条件分支。

### 7.5 HIGH：可变 mask 数与默认 DataLoader collate 不兼容

当 batch_size > 1 时，不同样本 `gt_masks` 形状 `[M_i,N]` 不同，默认 collate 无法 stack。当前 val batch_size=4 风险尤其高。

解决方案：

- 第一阶段固定每样本 M=2；
- 或实现 padded collate：`[B,M_max,N] + valid_mask[B,M_max]`；
- loss、metric、prompt sampling 全链路尊重 valid mask。

### 7.6 MEDIUM：本地默认 PartNeXt 路径与实际目录布局不一致

配置默认路径是：

```text
./data/partnext/dataset
```

当前本地数据实际是 HuggingFace DatasetDict 根目录：

```text
./data/partnext/dataset_dict.json
./data/partnext/train/
./data/partnext/test/
```

即正确根目录更可能是 `./data/partnext`。集群脚本通过参数覆盖为另一个 `.../dataset` 路径，因此集群可能正常，但本地默认配置会找不到数据。

建议统一数据路径约定，并在启动 preflight 中打印最终 resolved path 与数据集长度。

---

## 8. 显存问题与根因

### 8.1 CRITICAL：省显存配置仍然实例化 EVA Giant

`configs/hpp_memory_efficient.yaml` 将 Base backbone 覆盖写在顶层：

```yaml
pc_encoder:
  transformer:
    model_name: eva02_base_patch14_448
```

真正的实例化路径是：

```yaml
model:
  pc_encoder:
    transformer:
```

Hydra resolved config 已验证普通配置和省显存配置最终都仍使用 `eva_giant_patch14_224`。

修复：

```yaml
model:
  pc_encoder:
    transformer:
      model_name: eva02_base_patch14_448
      pretrained: false
```

修复后必须在模型实例化完成时打印：

- resolved model name；
- 总参数量与 trainable 参数量；
- 每模块参数量；
- precision；
- ZeRO stage。

### 8.2 HIGH：Mask Encoder 的 `B×M×L×K` 激活非常大

默认 KNN 配置近似：

```text
B = 1
M = 2 或 PartNeXt 的不定数量
L = 1024 patches
K = 256 neighbors
hidden = 128/256/512
```

典型中间张量为：

\[
[B M,L,K,C].
\]

例如 bf16 下 `[2,1024,256,256]` 单个张量约 256 MiB，前后还有 linear、concat、norm、GELU 和 autograd saved tensors。该路径在十轮中反复执行。

降低方法：

- 固定 M；
- 降低 L/K；
- Mask Encoder checkpoint；
- 按 mask 维度 chunk；
- 随机深度 rollout；
- 评估 Voronoi tokenizer，但将其视为架构变化。

### 8.3 HIGH：大量 `expand().clone()` 将广播真正实体化

为规避 FSDP/AMP 的 zero-stride view 问题，代码在 attention、mask encoder、prompt encoder 中频繁：

```python
x.expand(...).clone()
```

这虽然避免了某些 shard/view 问题，但会真正分配完整张量。例如：

- `[B,H,Nq,k,hD]` 的 query；
- `[B*M,N,3]` 的坐标；
- `[B*M,N,hidden]` 的 scatter index；
- 重复的 centers、position、temperature。

改进方向：

- 使用支持 broadcast 的算子，避免 clone；
- 使用扁平 offset index，而不是扩展到 hidden 维度的 int64 index；
- 按 query/key/mask chunk；
- 只在已确认会触发 FSDP 问题的最小边界 materialize；
- 为每个 materialization 写 shape 与字节数测试。

### 8.4 HIGH：attention chunk 仍预分配完整距离矩阵

当前 chunk 实现先分配：

\[
dist\in\mathbb{R}^{B\times H\times N_q\times N_k},
\]

然后仅分块计算临时 q/k。因此 chunk 只降低部分临时峰值，没有消除完整距离矩阵和 dense attention weight。

真正的 streaming softmax 需要在线维护：

- 每行 running max；
- exp sum；
- weighted value sum；

从而不保存完整 logits。局部 top-k 路径也不应再 scatter 回完整 `[B,Nq,Nk]` 权重，除非 debug 明确要求。

### 8.5 HIGH：ZeRO-3 主要解决模型状态，不解决十轮激活

ZeRO-3 可分片：

- 参数；
- 梯度；
- optimizer state。

但十轮 decoder、mask encoder、hyperbolic distance 的激活仍由每张卡保存。继续调小 ZeRO bucket 不能解决核心激活峰值，还可能增加通信抖动。

### 8.6 MEDIUM：ZeRO-3 leaf 不是激活显存方案

将 `MaskEncoder + HRA` 标为 leaf 可能减少动态子模块 hook 顺序不一致，但也会让整个 leaf 的参数一次性 gather，并增加瞬时驻留。

应在以下修复后再做 A/B：

1. 固定每 rank 的 M；
2. 修正 DeepSpeed loop；
3. 删除无条件同步；
4. 统一 forward 路径；
5. 测量 leaf on/off 的 peak allocated、reserved、吞吐和 collective 时间。

极小的 `stage3_max_live_parameters`、prefetch bucket 等也可能造成反复 gather/release，形成 thrashing 和 timeout。

### 8.7 MEDIUM：`use_cpu_offload` 并不是真正 activation offload

当前该选项主要执行：

```python
del tensor
torch.cuda.empty_cache()
gc.collect()
```

只要 autograd graph 或 `outputs` 仍引用 saved tensors，活跃显存就不能释放。`empty_cache()` 只能释放 caching allocator 中未使用的 block，不能释放活跃张量。

如果需要真正 activation offload，应使用：

- `saved_tensors_hooks/save_on_cpu`，但要避免与 checkpoint/ZeRO hook 冲突；
- DeepSpeed activation checkpoint/offload；
- 自定义 coarse-grained checkpoint；
- 随机深度 rollout，从根源上不构建多数轮计算图。

### 8.8 MEDIUM：allocator 配置只能处理碎片

`PYTORCH_CUDA_ALLOC_CONF`、`max_split_size_mb`、`expandable_segments` 只对碎片和动态尺寸有帮助，不能解决真实 live allocations。

应先区分：

- `allocated` 高：真实张量太多；
- `reserved >> allocated`：碎片或缓存问题；
- optimizer boundary 峰值：梯度/参数 gather；
- forward 第几轮增长：迭代图累积。

### 8.9 推荐的显存探针

在以下位置调用低频显存记录：

```python
torch.cuda.reset_peak_memory_stats()
allocated = torch.cuda.memory_allocated()
reserved = torch.cuda.memory_reserved()
peak_allocated = torch.cuda.max_memory_allocated()
peak_reserved = torch.cuda.max_memory_reserved()
```

建议阶段：

1. batch 输入上卡后；
2. pc encoder 后；
3. 每轮 mask encoder 后；
4. 每轮 HRA 后；
5. 每轮 decoder 后；
6. loss 后；
7. backward 后；
8. optimizer boundary 前后。

只在 rank 0、前几个 batch 或指定 debug step 输出，避免同步污染性能。

---

## 9. 测试存在的问题

### 9.1 当前 hyper ops 测试存在假阳性

测试辅助函数每次内部调用相同 `torch.manual_seed(0)`。因此 symmetry 测试中单独生成的 x/y 可能完全相同，triangle 测试的 x/y/z 也可能相同。

结果实际只验证：

- `d(x,x)=0`；
- 退化三角形；

没有真正验证不同点之间的对称性和三角不等式。

### 9.2 必须补充的单元测试

#### 基础几何

- 不同随机点的距离对称性；
- 三角不等式；
- `d(x,x)=0`；
- `exp0(log0(x))≈x`；
- `log0(exp0(v))≈v`；
- Möbius identity/inverse；
- 与 Geoopt 官方实现数值对照；
- `c→0` 时趋近欧氏运算。

#### 梯度

- float64 `gradcheck`；
- curvature 梯度非零；
- alpha/temp_a/HRA blocks 梯度有限；
- 靠近边界时无 NaN/Inf；
- bf16 前向与 float32 参考误差。

#### 模块路径

- diagonal HRA 可运行；
- block-diagonal HRA 可运行；
- q/k 投影后严格在球内；
- prompt branch 与 attention 共享曲率；
- ball-query 与 full attention 在 `k>=Nk` 时等价；
- checkpoint on/off 前向与梯度等价。

#### 训练循环

- DeepSpeed GAS=1/2/4 的 optimizer step 次数；
- DeepSpeed 与 DDP 在小模型上的参数更新近似一致；
- LR warmup 实际变化；
- 单 radius 不产生 NaN；
- 空 mask 被拒绝或重采；
- 不同 M 的 batch collate 行为明确。

---

## 10. 分阶段修复计划

### P0：建立可复现基线

目标：保证代码、数据、日志和配置可追踪。

- 补充 README：环境、数据目录、训练命令、评估命令；
- 打印 Hydra resolved config；
- 打印最终 backbone 名称和参数量；
- 固定 seed，同时记录每 rank seed；
- 记录 git commit、GPU、PyTorch、CUDA、DeepSpeed 版本；
- 增加 1 batch 单卡 smoke test；
- 增加 2 GPU GAS smoke test；
- 加显存阶段探针。

完成标准：相同 seed 的小数据实验能够复现 loss 前若干步。

### P1：训练正确性修复

优先级最高，暂不改方法公式。

1. 修正 DeepSpeed backward/step；
2. 接通 scheduler；
3. 修复 `radius.std(unbiased=False)`；
4. 修复固定 refinement RNG；
5. 禁止训练阶段静默 CUDA/Euclidean fallback；
6. 删除正式训练中的无条件 synchronize/print；
7. 对 DeepSpeed GAS 做 optimizer-step 单测。

完成标准：

- GAS=1/2/4 的参数更新方向一致；
- LR 按预期 warmup；
- 100 个 microbatch 无 NaN；
- global step 与 optimizer step 一致。

### P2：数据与随机 OOM 修复

1. PartNeXt 每样本先固定 M=2；
2. 修复 `RandomSampleMask` 的 replacement；
3. 使用 mask-aware 点采样；
4. 降采样后重新验证 mask；
5. 禁止空 GT；
6. 定义 empty IoU；
7. val batch_size 暂设 1，或实现 padded collate；
8. 记录 M、mask area、空 mask 率分布。

完成标准：完整遍历训练/验证数据各一遍，不发生 shape、empty mask 或 collate 错误。

### P3：几何一致性修复

1. 固定全局统一曲率；
2. 统一 q/k 的 `log0→linear→exp0/project`；
3. 修复 curvature tensor 梯度；
4. 删除无效 scalar `logit_scale`；
5. 正值参数化 `temp_a`；
6. 明确只在 prompt-to-point 注入 temperature，先建立稳定版本；
7. 删除 exp/log cancellation，明确 tangent value aggregation；
8. 修复 diagonal 路径；
9. 删除或重写 parallel transport；
10. 双曲核心 float32。

完成标准：所有几何单测、gradcheck、球内约束通过，fallback 计数为 0。

### P4：低风险显存优化

建议初始配置：

```text
backbone = EVA02 Base
num_points = 3072，必要时 2048
num_patches = 512
mask count M = 2
train batch per GPU = 1
val batch per GPU = 1
precision = bf16，双曲核 float32
ZeRO-3 = 仅处理模型状态
```

然后依次加入：

1. EVA block checkpoint；
2. Mask Encoder coarse-grained checkpoint；
3. Decoder layer checkpoint；
4. 移除不必要的 expand-clone；
5. mask 维度 chunk；
6. sparse/local attention 不返回 dense weights。

每次只开启一项并验证：

- forward 输出误差；
- 参数梯度误差；
- 峰值显存；
- tokens/sec；
- 是否出现 ZeRO/NCCL hang。

### P5：交互轮数显存优化

实现 random-depth rollout：

- eval 固定 10 轮；
- train 随机目标轮次；
- 前序 no-grad；
- 目标轮有梯度；
- 小比例 full-10 校准。

建议对照：

| 方案 | 训练图数量 | 科学含义 |
|---|---:|---|
| Full-10 | 10 | 原始完整监督 |
| Last-only-10 | 1 | 前9轮 rollout，监督第10轮 |
| Uniform-depth | 1 | 每 batch 随机监督 1～10 |
| 3-stage | 3 | 监督早/中/晚三个阶段 |
| Mixed | 1 为主，少量10 | 推荐折中 |

### P6：Voronoi 作为架构消融

Voronoi 1-NN + scatter 可以显著减少 `L×K` 激活，但会改变 tokenizer 的邻域归纳偏置，不属于纯工程等价优化。

必须对照：

- KNN vs Voronoi；
- 相同 backbone、patch 数、训练轮数；
- IoU@1/3/5/10；
- NoC85/90；
- 峰值显存；
- 吞吐；
- 小部件召回率。

---

## 11. 最低必要论文消融

为证明贡献来自双曲 HRA，而不是动态门控，至少需要：

| 编号 | 模型 |
|---|---|
| A | Point-SAM baseline / 标准 dot-product attention |
| B | 欧氏距离 `-β(r)||q-k||` |
| C | 双曲距离 + 固定 β |
| D | 双曲距离 + 普通 MLP 输出 β，无 HRA |
| E | HRA radius-conditioned geodesic attention |
| F | `c→0`、固定 c、共享可学习 c |

如果 E 只优于 A，却不优于 B/D，则主要贡献更可能来自动态 temperature，而不是双曲层级结构。

还应加入：

- shared vs independent curvature；
- prompt-to-point only vs bidirectional injection；
- tangent value vs true manifold aggregation；
- full-10 vs random-depth；
- KNN vs Voronoi；
- radius regularization on/off；
- Base vs Giant，避免规模混淆。

### 11.1 推荐指标

- IoU@1、IoU@3、IoU@5、IoU@10；
- NoC85、NoC90；
- mean IoU / category mIoU；
- 小/中/大 part 分层指标；
- 不同 hierarchy depth 指标；
- 参数量、FLOPs、峰值 allocated/reserved；
- train/inference throughput；
- 每次点击的 IoU gain；
- radius 与 part size/depth/click iteration 的相关性。

### 11.2 核心因果证据

论文应回答：

1. radius 是否真的与粒度相关？
2. radius 是否影响注意力熵？
3. attention entropy 的变化是否改善下一轮 IoU？
4. 双曲距离是否优于同样的欧氏距离门控？
5. 改善是否集中在深层级、小部件、后续点击？

---

## 12. 推荐的首轮可运行配置

在完成 P1～P3 后，建议从较保守配置启动：

```yaml
num_samples: 3072

train_dataloader:
  batch_size: 1

val_dataloader:
  batch_size: 1

gradient_accumulation_steps: 8
mixed_precision: bf16

model:
  prompt_iters: 3  # 仅用于最初正确性 smoke；正式研究改 random-depth 1..10
  pc_encoder:
    patch_embed:
      num_patches: 512
      patch_size: 64
    transformer:
      model_name: eva02_base_patch14_448
  mask_decoder:
    transformer:
      depth: 4
      k_neighbors: 32
```

数据侧固定每样本两个有效 mask。该配置的目标不是最终精度，而是先证明：

- loss 正常下降；
- LR 正常变化；
- radius/temperature 有有限梯度；
- 没有 fallback；
- 显存阶段曲线可解释；
- 多卡 optimizer step 一致。

之后再逐项恢复 10 轮交互、更大点数、更多 mask 或 Giant backbone。

---

## 13. 修复完成检查表

### 训练正确性

- [ ] DeepSpeed 每 microbatch 都调用 `backward` 与 `step`
- [ ] DeepSpeed loss 不手动除 GAS
- [ ] optimizer step 次数符合预期
- [ ] scheduler 实际 warmup/decay
- [ ] radius diversity 单元素不 NaN
- [ ] refinement iteration 不是固定常数
- [ ] 正式训练无无条件 CUDA synchronize

### 数据

- [ ] PartNeXt M 有上限
- [ ] RandomSampleMask 可处理 mask 数不足
- [ ] 点采样后无空 GT
- [ ] empty IoU 有明确语义
- [ ] val collate 可处理 mask 形状
- [ ] resolved dataset path 正确

### 几何

- [ ] 全模型曲率一致
- [ ] 双曲 q/k 投影后在球内
- [ ] curvature 梯度完整
- [ ] temp_a 保证正值
- [ ] 无效 logit_scale 已删除或重定义
- [ ] value aggregation 的数学表述与实现一致
- [ ] point-to-prompt temperature 语义明确
- [ ] diagonal HRA 路径可运行
- [ ] parallel transport 正确或未使用
- [ ] 训练 fallback 计数为 0

### 显存

- [ ] memory-efficient 配置实际使用 Base
- [ ] 每阶段显存探针已加入
- [ ] checkpoint on/off 梯度等价
- [ ] 十轮训练不再同时保留十个图，或已有足够显存证据
- [ ] expand-clone 热点已量化
- [ ] attention 不无条件返回 dense weights
- [ ] ZeRO leaf 有实测而非仅凭注释启用

### 论文证据

- [ ] 欧氏动态温度控制组
- [ ] 固定双曲温度控制组
- [ ] 无 HRA 控制组
- [ ] fixed/shared learnable curvature
- [ ] KNN/Voronoi 消融
- [ ] full-10/random-depth 消融
- [ ] radius 与真实粒度相关性
- [ ] 报告峰值显存、吞吐与多点击指标

---

## 14. 参考资料

1. Ganea, Bécigneul, Hofmann. **Hyperbolic Neural Networks**. NeurIPS 2018.  
   https://papers.neurips.cc/paper/7780-hyperbolic-neural-networks.pdf
2. Geoopt 官方 Poincaré/Stereographic 数学实现。  
   https://github.com/geoopt/geoopt/blob/master/geoopt/manifolds/stereographic/math.py
3. HyperET：HRA 与双曲参数高效微调。  
   https://openreview.net/forum?id=NM8Apk61NA  
   https://github.com/godlin-sjtu/HyperET
4. Segment Anything 补充材料：交互轮次与 mask 采样。  
   https://openaccess.thecvf.com/content/ICCV2023/supplemental/Kirillov_Segment_Anything_ICCV_2023_supplemental.pdf
5. Point-SAM 官方仓库与交互训练实现。  
   https://github.com/zyc00/Point-SAM
6. DeepSpeed 官方训练 API。  
   https://deepspeed.readthedocs.io/en/stable/training.html
7. PyTorch Activation Checkpoint 官方文档。  
   https://docs.pytorch.org/docs/stable/checkpoint
8. Hyperbolic Image Segmentation, CVPR 2022.  
   https://openaccess.thecvf.com/content/CVPR2022/papers/Atigh_Hyperbolic_Image_Segmentation_CVPR_2022_paper.pdf
9. Rotation-Invariant Hierarchical Segmentation on Poincaré Ball for 3D Point Clouds, ICCVW 2023.  
   https://openaccess.thecvf.com/content/ICCV2023W/SHARP/papers/Onghena_Rotation-Invariant_Hierarchical_Segmentation_on_Poincare_Ball_for_3D_Point_Cloud_ICCVW_2023_paper.pdf
10. Hyperbolic Part-Whole Segmentation, AISTATS 2026.  
    https://openreview.net/forum?id=wNwJTwpLio

---

## 15. 最终建议

这个方向值得继续实现，但不建议直接在当前版本上投入长时间多卡训练。最稳妥的路线是：

1. 先修 DeepSpeed、scheduler、NaN 与数据约束；
2. 再统一曲率和 q/k 流形投影；
3. 用 Base + M=2 + 3072 点建立稳定基线；
4. 加显存探针和 checkpoint；
5. 实现 random-depth rollout；
6. 最后再做 Voronoi、可学习曲率、真正流形聚合和 Giant backbone。

只有在“欧氏动态温度”控制组下仍有稳定优势，并且 radius 与真实 part 层级/面积/点击增益存在一致相关性时，HPP-SAM 的核心论点才足够可信。
